{% extends "base.html.twig" %}

{% block title %}Ja no definitely - Home {% endblock %}

{% block body %}
<div>

    <!--Questions-->
    <div id="content" class="mui-container-fluid">
    <br>
      <div class="mui-row">
        <div class="mui-col-sm-10 mui-col-sm-offset-1">

          {{ form_start(questionForm, {'attr': {'novalidate': 'novalidate'}}) }}
          {{ form_widget(questionForm) }}
          {{ form_end(questionForm) }}
          <br>

          <br>
          <br>
          {# <div class="mui--text-black-54 mui--text-body2">{{ category.category_name }}</div>
          <div class="mui-divider"></div>
          <br>
          <div class="mui--text-headline test">{{ question.question_text }}</div>
          <div class="mui--text-black-54">Asked By <a href="#">{{ question.question_author }}</a> on {{ question.question_date }}</div>
          <br>
          <div> <a>See answers</a> </div>
          <br> #}

          <ul>
          {% for questionId in question %}
            <li>{{ questionId.question_text }}</li>
          {% endfor %}
          </ul>

          <div class="mui--text-black-54 mui--text-body2">Category</div>
          <div class="mui-divider"></div>
          <br>
          <div class="mui--text-headline">Question</div>
          <br>
          <div class="mui--text-black-54">Asked By <a href="#">User Name</a> x days ago</div>
          <div class="mui-divider"></div>
          <br>
          {{ form_start(answerForm, {'attr': {'novalidate': 'novalidate'}}) }}
          {{ form_widget(answerForm) }}
          {{ form_end(answerForm) }}
          <br>

          <div class="mui--text-black-54 mui--text-body2">Category</div>
          <div class="mui-divider"></div>
          <br>
          <div class="mui--text-headline">Question</div>
          <br>
          <div class="mui--text-black-54">Asked By <a href="#">User Name</a> x days ago</div>
          <br>

          <div class="mui--text-black-54 mui--text-body2">Category</div>
          <div class="mui-divider"></div>
          <br>
          <div class="mui--text-headline">Question</div>
          <br>
          <div class="mui--text-black-54">Asked By <a href="#">User Name</a> x days ago</div>
          <br>
          
      </div>
    </div>

    {# ads #}
  <div id="ad" class="mui-container-fluid">
      <div class="mui-row">
        AD!
    </div>
</div>

</div>
{% endblock %}